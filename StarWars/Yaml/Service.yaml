apiVersion: v1
kind: Service
metadata:
  labels:
    app: starwars
    component: frontend
  name: frontend
spec:
  ports:
  - port: 80
    targetPort: 8080
  selector:
    app: starwars
    component: frontend
  type: LoadBalancer
